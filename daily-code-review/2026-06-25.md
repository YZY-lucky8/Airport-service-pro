# Daily Code Review — 2026-06-25

> 审核时间: 2026-06-25 17:00 (Asia/Shanghai)
> 仓库: Airport-service-pro

---

## 1. 本地状态检查

- `git status --short`: 仅存在 untracked 目录 `daily-code-review/`，无未提交的本地修改
- ✅ 可以安全执行后续操作

## 2. 远程分支新提交（过去 24 小时）

| 分支 | 新提交数（24h） | 最近提交时间 |
|------|----------------|-------------|
| `yangziyu/rate-limit` | **0** | 2026-04-18（约 67 天前） |
| `杨梓瑜/后端` | 0 | 与 master 一致 |
| `段琼馨/前后端` | **0** | 2026-05-05（约 50 天前） |
| `王晓恩/admin-page` | 0 | 与 master 一致 |

**结论: 过去 24 小时内没有任何分支有新提交。** 但仍对积压的未合并分支进行了审查。

## 3. 分支差异概览

### yangziyu/rate-limit（2 个未合并提交）
- `b056e3b` 修复 app.js（2026-04-18）
- `394aa14` 修改静态文件路径为相对路径（2026-04-18）

**变更统计:**
```
backend/app.js | 787 ++++++++++++++++++++++--- (781 insertions, 7 deletions)
public         |   1 +
```

### 段琼馨/前后端（5 个未合并提交）
- `2f48a61` Merge branch ...（2026-05-05）
- `c4e6bc6` Merge branch 'yangziyu/rate-limit'（2026-05-05）
- `4b1ff89` Merge branch ...（2026-04-19）
- `999ad5b` 令牌防护（2026-04-19）
- `d92eab6` 令牌防护（2026-04-19）

**变更统计（19 个文件，+4843 / -404 行）:**
- `backend/app.js`（+354）
- `backend/routes/security.js`（+1032，新文件）
- `frontend/security/assets/security.html`（+2628，新文件）
- `frontend/public/test.html`（+424，新文件）
- `database/schema.sql`（+184，新文件）
- `database/TEST.sql`（+66，新文件）
- `package.json/lock`（+150）
- 其他: .idea/ 配置、删除旧前端页面等

### 杨梓瑜/后端 & 王晓恩/admin-page
已与 master 同步，无差异。

## 4. 代码审查发现

### 🔴 严重安全问题

**S1. 硬编码管理员凭据**（段琼馨/前后端 — `backend/routes/security.js:983-985`）
```javascript
if (username === 'admin' && password === 'admin') {
    token: 'mock-jwt-token-' + Date.now(),
}
```
- ⚠️ 使用明文 `admin/admin` 作为管理员凭据
- ⚠️ 返回 mock JWT token，无实际认证机制
- **建议:** 使用环境变量或数据库存储凭据；实现真实的认证流程

**S2. 硬编码数据库密码**（段琼馨/前后端 — `backend/routes/security.js:22`）
```javascript
password: 'Airport123!',   // 修改为你的密码
```
- ⚠️ 数据库密码硬编码在源码中
- **建议:** 使用 `process.env.DB_PASSWORD`，统一读取环境变量

**S3. 循环依赖风险**（段琼馨/前后端 — `backend/routes/security.js:14-17`）
```javascript
try {
  const app = require('../app');
  var pool = app.pool;
} catch (e) { ... }
```
- ⚠️ `security.js` 在 `app.js` 加载时被 require，形成循环依赖
- ⚠️ fallback 逻辑会创建第二个数据库连接池
- **建议:** 将数据库连接池抽离为独立模块 `db.js`

### 🟡 中等风险

**M1. 布隆过滤器误判**（yangziyu/rate-limit — `backend/app.js`）
- 布隆过滤器无法删除条目，误判率虽仅 0.001 但无法回退
- 黑名单 IP `127.0.0.2` 和 `::1` 是本地回环地址，可能误伤开发者
- **建议:** 改用 Set 或数据库存储黑名单

**M2. 速率限制过于严格**（yangziyu/rate-limit — `backend/app.js`）
- 配置: 1 秒内最多 5 个请求
- 在现代 SPA 中（含静态资源、API 预检等），此限制可能误伤正常用户
- **建议:** 至少放宽到 20-30 请求/秒，或针对 API 路径单独限制

**M3. 内存泄漏风险**（yangziyu/rate-limit — `backend/app.js`）
```javascript
const requestCounts = new Map();
```
- 滑动窗口 Map 无限增长，无 TTL 清理机制
- **建议:** 定期清理过期 IP 的 Map 条目（如每 5 分钟）

**M4. 安全路由中的 catch(e) {} 空异常处理**（段琼馨/前后端 — `backend/routes/security.js`）
- 多处 `catch (e) {}` 静默吞掉异常，无法排查线上问题
- **建议:** 至少记录 `console.error(e)`

### 🟢 低优先级

**L1. 开发配置文件混入仓库**（`.idea/` 目录）
- 建议加入 `.gitignore`

**L2. `database/TEST.sql` 命名不规范**
- 建议移入 `test/` 目录或使用 `database/tests/` 前缀

**L3. `frontend/public/test.html` 不应提交**
- 测试页面不应进入主仓库

## 5. 合并尝试结果

| 分支 | 合并结果 | 说明 |
|------|---------|------|
| `杨梓瑜/后端` | ✅ 已同步 | 与 master 一致 |
| `王晓恩/admin-page` | ✅ 已同步 | 与 master 一致 |
| `yangziyu/rate-limit` | ❌ 有冲突 | `backend/app.js` 内容冲突 |
| `段琼馨/前后端` | ❌ 有冲突 | `backend/app.js`、`database/schema.sql`、`frontend/security/assets/security.html`、`package-lock.json`、`package.json` 共 5 个文件冲突 |

**冲突已回退（`git merge --abort`），未提交任何 merge commit。**

## 6. Push 状态

- `git push origin master`: **Everything up-to-date**（master 无新提交）

## 7. 待办事项 / 建议

1. **🔴 紧急**: 修复 `security.js` 中的硬编码凭据（admin/admin + Airport123!）
2. **🟡 高优**: 解决两个分支的 merge 冲突，建议先修复冲突再合并
3. **🟡 高优**: 将布隆过滤器黑名单改为可动态管理的数据结构
4. **🟢 建议**: 添加 CI/CD 检查，防止硬编码凭据进入 master
5. **🟢 建议**: 将数据库连接池独立为 `db.js` 模块，避免循环依赖

---
*自动生成 by OpenClaw daily-code-review cron job*
