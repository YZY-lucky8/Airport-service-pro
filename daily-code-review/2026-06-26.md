# 每日代码审核报告 — 2026-06-26

## 一、仓库状态

| 检查项 | 结果 |
|--------|------|
| 本地未提交修改 | 无（仅 `daily-code-review/` 未跟踪目录） |
| `git fetch --all` | 成功 |
| `git push origin master` | Everything up-to-date |

## 二、24小时内新提交

**过去 24 小时内所有分支均无新提交。**

## 三、历史未合并分支审查

### 1. `yangziyu/rate-limit`（2 个未合并提交，2026-04-18）

**变更范围：**
```
backend/app.js | 787 +++++++++++++++++-
public         |   1 +
2 files changed, 781 insertions(+), 7 deletions(-)
```

**关键代码审查：**

- ✅ **BloomFilter IP 黑名单**：实现正确，使用双重哈希 + 动态位数组。但黑名单为硬编码 IP（`192.168.1.100` 等），生产环境应改为数据库读取。
- ✅ **滑动窗口频率限制**：1秒内最多5个请求，逻辑正确。但 `requestCounts` 使用内存 Map，**存在内存泄漏风险**——没有定期清理不活跃 IP 的过期条目（虽然每次访问会清理旧时间戳，但不会清理空数组的 IP 键）。
- ⚠️ **安全警告 — 硬编码数据库密码**：`backend/app.js` 中有 `password: 'Airport123!'`，应使用环境变量 `.env`。
- ⚠️ **安全警告 — `trust proxy: true` 无条件信任**：设置 `app.set('trust proxy', true)` 会在非代理环境下导致攻击者可伪造 X-Forwarded-For 绕过 IP 限制。应根据部署环境配置。
- ⚠️ **安全警告 — `attackMonitor` 内存 Map 无上限**：暴力破解和速率限制的 Map 仅定期清理（5分钟），高频攻击时可耗尽内存。
- ℹ️ **代码重复**：`require('dotenv').config()` 在文件顶部调用了两次。

**合并尝试：** ❌ 冲突（`backend/app.js` 与 master 有大量重叠修改）

---

### 2. `段琼馨/前后端`（5 个未合并提交，2026-04-19 至 2026-05-05）

**变更范围：**
```
19 files changed, 4843 insertions(+), 404 deletions(-)
```

**关键代码审查：**

#### 🔴 严重问题

1. **`backend/app.js` 存在未解决的 Git 合并冲突标记**：
   文件中包含 `<<<<<<< HEAD` / `=======` / `>>>>>>> 940c284...` 冲突标记。这是一个严重的 Bug——如果直接部署，`require()` 解析会报错，应用无法启动。

2. **硬编码数据库密码**：`backend/routes/security.js` 中包含：
   ```js
   password: 'Airport123!',   // 修改为你的密码
   database: 'airport_db',    // 修改为你的数据库名
   ```
   密码硬编码在路由文件中，应统一使用环境变量。

3. **循环依赖风险**：`security.js` 中 `require('../app')` 尝试从 `app.js` 获取 pool，而 `app.js` 又 `require('./routes/security')`，可能导致 `pool` 为 `undefined`。

#### 🟡 中等问题

4. **SQL 注入风险（低）**：`security.js` 中部分查询使用字符串拼接（如 `"WHERE DATE(created_at) = CURDATE()"`），当前使用参数化查询为主，但需统一审查。

5. **`intercept-trend` 接口返回硬编码模拟数据**：`latest-logs` 和 `intercept-trend` 返回的是硬编码的 mock 数据，应在生产环境替换为真实数据库查询。

6. **`database/schema.sql` 包含敏感测试数据**：表结构中包含模拟的身份证号（`110101199001011234` 等）和护照号。虽为测试数据，但不应提交到版本控制。

7. **`.idea/` IDE 配置被提交**：IntelliJ/WebStorm 配置文件不应进入版本控制，应在 `.gitignore` 中排除。

#### 🟢 正面改进

8. ✅ **HMAC 令牌系统**：`verifyToken` 使用 `crypto.timingSafeEqual()` 防止时序攻击，令牌有 TTL 和一次性使用机制（`isTokenUsed`），实现较好。

9. ✅ **输入验证中间件 `validateInput`**：提供字段类型、长度、正则验证，应用于 `/api/check-in` 端点。

10. ✅ **空值安全**：大量使用可选链（`?.`）和默认值（`|| 0`），防止 `undefined` 解构错误。

11. ✅ **`/health` 健康检查端点**：提供应用健康状态。

**合并尝试：** ❌ 冲突（`backend/app.js`、`database/schema.sql`、`frontend/security/assets/security.html`、`package-lock.json`、`package.json`）

---

### 3. `杨梓瑜/后端`、`王晓恩/admin-page`

无未合并到 master 的提交。

## 四、合并结果总结

| 分支 | 未合并提交数 | 合并结果 | 原因 |
|------|-------------|---------|------|
| yangziyu/rate-limit | 2 | ❌ 冲突 | backend/app.js 冲突 |
| 段琼馨/前后端 | 5 | ❌ 冲突 | 多文件冲突，且分支内有未解决的合并标记 |
| 杨梓瑜/后端 | 0 | ✅ 已同步 | 无需合并 |
| 王晓恩/admin-page | 0 | ✅ 已同步 | 无需合并 |

## 五、建议

1. **立即修复**：`段琼馨/前后端` 分支中 `backend/app.js` 的 Git 合并冲突标记
2. **安全加固**：移除所有硬编码密码，统一使用 `.env` 环境变量
3. **安全加固**：`trust proxy` 应根据部署环境配置，不应无条件信任
4. **架构改进**：`security.js` 中的循环依赖应重构，使用共享的数据库连接模块
5. **代码质量**：清理 `.idea/` 等 IDE 配置，更新 `.gitignore`
6. **合并策略**：建议开发者先 rebase 到最新 master 后再提 merge

---
*报告生成时间：2026-06-26 17:00 CST*
*自动审核脚本 v1.0*
