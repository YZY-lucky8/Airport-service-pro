# 恒盾智航 — 基于国产化智能体的机场自助终端安全防护系统

> **参赛类别：** 信息安全作品赛
> **所属团队：** 中国民航大学
> **版本：** v1.1（2026-06-29 最终版）

---

## 一、项目概述

### 1.1 项目名称

**恒盾智航**——基于国产化智能体的机场自助终端安全防护系统

### 1.2 执行摘要

随着我国民航业智能化进程加速，机场自助服务终端数量持续增长。然而，大量存量终端面临**安全防护能力缺失**和**适老化交互体验差**两大突出问题。本项目提出了一套纯软件、轻量化的升级方案，无需硬件改造即可为机场自助终端同时提供企业级安全防护和智能化旅客服务。

核心创新在于：**首次将国产化智能体技术引入机场终端安全防护领域**，构建了 6 项轻量化 DDoS 防护机制 + 3 道智能体安全防线的纵深防御体系，实现了安全分析、旅客服务、智能运维三位一体。所有核心代码自主编写，可运行于银河麒麟操作系统，满足民航自主可控合规要求。

系统已在腾讯云公网完成部署验证，1000 并发压测响应 ≤ 300ms，防护性能损耗仅 7.1%。163 项自动化测试通过率 95.3%，10 类攻防演练全部成功防御，具有明确的工程应用价值。

### 1.3 项目定位

一套面向机场自助服务终端的**纯软件、轻量化、可部署于存量终端**的一体化升级方案，同时解决"适老化交互体验差"和"安全防护能力缺失"两大行业痛点，并首次将**国产化智能体（Agent）技术**引入机场终端安全防护领域。

**类比定位：** 给机场老旧自助终端装上"智能座舱"和"安全气囊"。

**核心声明：**
- **不是** 硬件设备，不替换机场核心系统
- **是** 纯软件、零硬件改造、可平滑部署的升级包
- 所有核心代码自主编写，不依赖 LangChain / Dify 等第三方智能体平台
- 可运行于国产操作系统（银河麒麟），满足自主可控合规要求

### 1.4 核心价值主张 — "智、适、安"三位一体

| 维度 | 内容 | 技术体现 |
|------|------|---------|
| **智** | 国产化智能体驱动的安全分析 + 旅客服务 | 自研 Agent 调度框架、三级知识库检索、意图理解、情感分析、智能阈值调整 |
| **适** | 一键老年模式，全链路适老化设计 | 字体放大 30%、按钮 ≥ 44×44px、对比度 ≥ 4.5:1，符合 WCAG AA 级标准 |
| **安** | 6 项轻量化 DDoS 防护 + 3 道 Agent 安全防线 | 滑动窗口限流、布隆过滤器、HMAC 令牌、输入过滤、运行时权限验证、输出过滤 |

---

## 二、行业背景与问题定义

### 2.1 行业痛点

1. **机场自助终端安全防护缺位。** 机场自助值机/查询终端暴露于公共网络，面临 DDoS 攻击、暴力破解、重放攻击等威胁，但现有方案多为重型硬件防火墙，中小机场无力承担。
2. **适老化交互体验差。** 老龄化社会中，大量老年旅客无法独立完成自助值机流程，现有终端交互设计对老年人不友好。
3. **安全运维依赖人工经验。** 机场安全运维人员需要实时分析攻击日志、手动调整防护阈值，工作量大且容易疏漏。
4. **国产化替代需求迫切。** 民航安全基础设施亟需自主可控的替代方案，但现有智能体框架（LangChain、CrewAI 等）均为海外产品，无法满足数据安全和合规要求。

### 2.2 政策依据

本项目的提出与设计遵循以下国家和行业政策：

| 政策文件 | 关联维度 | 关联内容 |
|----------|---------|---------|
| 《中华人民共和国网络安全法》 | 安全合规 | 关键信息基础设施运营者需履行安全保护义务，机场终端作为旅客数据入口纳入保护范围 |
| 《中华人民共和国数据安全法》 | 数据安全 | 旅客身份、航班等个人信息属于重要数据，需实现本地化存储和处理 |
| 《关键信息基础设施安全保护条例》 | 安全合规 | 民航领域属于关键信息基础设施，安全系统需满足自主可控要求 |
| 《"十四五"民航高质量发展专项规划》 | 行业需求 | 明确提出推进机场智能化改造、适老化改造 |
| 《互联网网站适老化通用设计规范》（工信部 2021） | 适老化 | 规定适老化交互标准（字号、对比度、按钮尺寸等），本项目完全遵循 |
| 《国家信息化发展战略纲要》/ 信创战略 | 国产化 | 关键信息系统逐步实现国产替代，智能体框架不得依赖海外产品 |
| 《无障碍环境建设法》（2023） | 适老化 | 公共场所服务设施应提供无障碍和适老化服务 |

### 2.3 问题定义

**核心问题：** 如何在**零硬件改造、零第三方平台依赖**的前提下，为机场存量自助终端提供企业级的安全防护能力和智能交互服务，同时满足国产化合规要求？

**本项目回答：** 通过自研的轻量化智能体框架 + 6 项 DDoS 防护机制 + 可插拔 LLM 架构，实现了安全、体验、国产化的三位一体解决方案。

---

## 三、系统架构

### 3.1 整体架构（云-边-端三层）

```
┌──────────────────────────────────────────────────────────────┐
│                    终端层 (Terminal)                          │
│   旅客端 HTML/CSS/JS 界面 ｜ 语音采集 ｜ 触控交互 ｜ 老年模式   │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP / WebSocket
┌──────────────────────────▼───────────────────────────────────┐
│                    边缘层 (Edge — Node.js)                     │
│  ┌─────────────┐ ┌──────────────┐ ┌────────────────────────┐ │
│  │ 6项DDoS防护  │ │ JWT认证中间件  │ │     智能体调度核心      │ │
│  │ 中间件链      │ │ (零依赖自实现) │ │  (AgentDispatcher)     │ │
│  └─────────────┘ └──────────────┘ └───────────┬────────────┘ │
│                                                │              │
│              ┌─────────┬─────────┬─────────────┤              │
│              ▼         ▼         ▼             ▼              │
│         Guardrail  Classifier  Knowledge   Emotion           │
│         (输入过滤)  (意图分类)  Retriever   Module            │
│                          │              (情感分析)             │
│                    Security Mgmt      LLM Adapter            │
│                    Agent             (可插拔LLM)             │
│                          │                                        │
│              ┌───────────▼──────────────────────────┐            │
│              │       18个RESTful API 接口             │            │
│              │  旅客端 / 管理端 / 安全端 / Agent端     │            │
│              └───────────┬──────────────────────────┘            │
└──────────────────────────┼──────────────────────────────────────┘
                           │ SQL
┌──────────────────────────▼──────────────────────────────────────┐
│                    云端/数据层 (Cloud)                            │
│   MySQL 8.0（生产） / SQLite（开发降级）                           │
│   核心表 × 10 ｜ Agent扩展表 × 5 ｜ 初始数据 × 50+                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 终端层 | 原生 HTML5 / CSS3 / JavaScript | 零框架依赖，兼容所有现代浏览器 |
| 边缘层 | Node.js + Express | 后端运行时，18 个 API 接口 |
| 数据库 | MySQL 8.0 / SQLite（双适配） | 通过 `db-adapter.js` 自动降级 |
| 认证 | 自研 JWT（纯 Node.js crypto） | HMAC-SHA256 签名，零外部依赖 |
| DDoS 防护 | 自研中间件（6 项机制） | 滑动窗口、Bloom Filter、HMAC 等 |
| 智能体框架 | 自研 AgentDispatcher | 状态机驱动，参考 LangGraph 理念 |
| LLM 适配器 | 可插拔架构 | 支持 rule_engine / OpenAI / vLLM / DashScope |
| 前端图表 | ECharts | 安全态势可视化 |

---

## 四、核心技术创新

### 4.1 创新一：6 项轻量化 DDoS 防护机制（核心安全能力）

针对机场自助终端算力有限的特点，设计了纯软件、低开销的防护体系：

| # | 机制 | 技术原理 | 性能影响 |
|---|------|---------|---------|
| 1 | **滑动窗口频率检测** | 2 秒窗口内 IP 请求计数，超 15 次拦截 | ≈ 0.5ms/请求 |
| 2 | **布隆过滤器 IP 黑名单** | 概率型数据结构，10000 位集合，误判率 0.001 | ≈ 0.1ms/查询 |
| 3 | **HMAC 一次性令牌** | HMAC-SHA256 签名，300 秒有效，防重放攻击 | ≈ 0.3ms/验证 |
| 4 | **攻击反馈隐藏** | 所有拦截统一返回 403，攻击者无法探测规则 | 0 |
| 5 | **混合防御白名单** | 内网 IP 自动跳过限流，管理操作不受限 | ≈ 0.2ms/查询 |
| 6 | **关键服务免扰** | 紧急帮助/医疗急救路径直接放行，保障生命安全通信 | 0 |

**创新点：** 6 项机制组合成纵深防御链，整体性能损耗仅 **7.1%**，在 1000 并发压测下响应 ≤ 300ms，成功率 ≥ 99.2%。

### 4.2 创新二：自研国产化智能体调度框架（核心 AI 能力）

参考 LangGraph 有向图状态机和 VeriGuard 形式化验证理念，自研了一套适用于机场终端的轻量级智能体框架：

#### 4.2.1 状态机架构

```
INIT → GUARDRAIL_CHECK → INTENT_CLASSIFY → RUNTIME_CHECK
  → ACTION_ROUTE → TOOL_EXEC → OUTPUT_FILTER → DONE
  任何节点失败 → ERROR_HANDLER → DONE
```

每个节点均有审计日志记录，全程可追溯。

#### 4.2.2 三大安全防线

| 防线 | 位置 | 功能 | 技术实现 |
|------|------|------|---------|
| **防线 1：输入过滤** | 请求入口 | 拦截 Prompt Injection、SQL 注入、XSS、PII 脱敏 | `InputGuardrail`，40+ 检测规则，覆盖中英文注入模式 |
| **防线 2：运行时监控** | 动作执行前 | 权限验证 + 动作白名单 | 最小权限原则，仅允许 10 类预定义动作 |
| **防线 3：输出过滤** | 响应输出前 | 防止内部信息泄露 | 过滤 system prompt 泄露、内部配置暴露 |

#### 4.2.3 感知模块（Perception）

- **意图分类：** 8 类预定义意图（登机口导航、行李查询、航班查询、知识问答、行程建议、地点导航、值机选座、闲聊），采用 Constrained Decoding 方式
- **情感识别：** 5 类情感（焦虑/愤怒/困惑/高兴/恐惧），强度 1-10 评分
- **实体抽取：** 航班号、登机口、座位偏好、地点、时间预算等
- **多意图融合：** 高情感 + 业务意图 → 复合意图（如 `emotion_support+gate_navigation`）

#### 4.2.4 可插拔 LLM 适配器

支持 5 种 LLM 后端，运行时动态切换：

| 提供者 | 说明 | 适用场景 |
|--------|------|---------|
| `rule_engine` | 基于规则的本地引擎，零依赖 | 默认模式，离线可用 |
| `openai` | OpenAI API (GPT-4o) | 云端推理 |
| `vllm` | 本地 vLLM 部署 (Qwen 等) | 国产化本地推理 |
| `dashscope` | 阿里云通义千问 | 国产云端推理 |
| `custom` | 兼容 OpenAI 格式的自定义 API | 灵活扩展 |

**创新点：** 不依赖 LangChain / CrewAI / AutoGen 等第三方平台，所有代码自主编写，满足数据不出站、国产化合规要求。

### 4.3 创新三：三级知识库检索引擎（轻量 RAG）

实现了一套轻量级 RAG（检索增强生成）系统：

| 级别 | 检索方式 | 权重 | 说明 |
|------|---------|------|------|
| 第一级 | 关键词精确匹配 | × 3 | 分词 + 停用词过滤 + 关键词字段匹配 |
| 第二级 | 模糊匹配（LIKE） | × 1.5 | 标题/内容/关键词模糊搜索 |
| 第三级 | 全文检索（FULLTEXT） | × 1 | MySQL 全文索引 |

- 知识库含 **12 条机场专业知识**（充电宝、行李、安检、停车、航班服务等），5 大分类
- 支持 **15 条 POI 设施数据**（餐厅、商店、卫生间、安检口等），支持附近推荐

### 4.4 创新四：智能安全阈值动态调整

安全管理智能体自动分析攻击日志比例，动态建议防护阈值调整：

| 场景 | 攻击占比 | 建议动作 | 阈值 |
|------|---------|---------|------|
| 正常运行 | < 5% | 放宽 | 50 次/秒 |
| 轻微攻击 | 5%-20% | 保持 | 当前值 |
| 明显攻击 | 20%-50% | 收紧 | 20 次/秒 |
| 紧急攻击 | > 50% | 紧急 | 5 次/秒 |

- 置信度计算考虑数据量大小和攻击占比极端程度
- 调整需管理员审批，记录完整审计日志
- 支持人工/Agent 两种管理模式切换

### 4.5 创新五：情感感知旅客服务

- 检测旅客情绪（焦虑/愤怒/困惑/高兴/恐惧），强度 1-10
- 高情感（≥ 5）时自动切换安抚策略（"先安抚情绪，再处理问题"）
- 5 类情感各有专属安抚话术模板
- 情感日志自动记录，支持后台统计分析

---

## 五、系统实现情况

### 5.1 代码规模与质量

| 指标 | 数值 |
|------|------|
| 后端代码 | Node.js，约 1.5 万行 |
| 前端页面 | 3 个（旅客端 / 管理端 / 安全端） |
| API 接口 | 18 个 RESTful 接口 + 10+ 个 Agent API |
| 数据表 | 核心表 10 张 + Agent 扩展表 5 张 |
| 安全中间件 | 6 个防护模块 + JWT 认证 |
| 测试用例 | 163 项，覆盖率 93%，通过率 95.3% |

### 5.2 核心文件结构

```
Airport-service-pro/
├── backend/
│   ├── app.js                  # 主入口，整合所有安全模块
│   ├── db-adapter.js           # 数据库适配层（MySQL/SQLite 双模式）
│   ├── routes/agent.js         # Agent 系统 API 路由
│   ├── agent/
│   │   ├── dispatcher.js       # 智能体核心调度器（状态机）
│   │   ├── guardrail.js        # 输入过滤（40+ 检测规则）
│   │   ├── intent-classifier.js # 意图分类 + 情感识别 + 实体抽取
│   │   ├── knowledge-retriever.js # 三级知识库检索
│   │   ├── emotion-module.js   # 情感分析与安抚
│   │   ├── security-management.js # 智能阈值调整
│   │   ├── llm-adapter.js     # 可插拔 LLM 适配器
│   │   └── itinerary-advisor.js # 行程建议生成
│   └── middleware/
│       └── auth.js             # JWT 认证（纯 Node.js 实现）
├── frontend/
│   ├── passenger/index.html    # 旅客端
│   ├── admin/                  # 管理后台
│   └── security/               # 安全管理端
├── database/
│   ├── schema.sql              # 核心表结构
│   └── agent_schema.sql        # Agent 扩展表
├── docs/
│   └── secure-agent-architecture-report.md  # 安全智能体架构报告
└── daily-code-review/          # 每日代码审核记录
```

### 5.3 性能指标

| 指标 | 数值 | 测试方法 |
|------|------|---------|
| 并发能力 | 1000 并发 | Apache Bench（ab）HTTP 压力测试 |
| 平均响应时间 | ≤ 300ms | ab -c 1000 -n 5000，P99 延迟 < 500ms |
| 成功率 | ≥ 99.2% | 5000 请求中失败数 < 8% |
| 防护性能损耗 | 仅 7.1% | 开启/关闭防护中间件对比测试 |
| Agent 处理延迟 | 规则引擎 < 5ms | 内置计时器，1000 次调用取平均 |
| 布隆过滤器内存 | 12KB/万条 IP | process.memoryUsage() 监控 |

### 5.4 安全攻防演练

为验证系统安全性，设计了完整的攻防演练方案：

#### 5.4.1 攻击面与防御验证

| 攻击类型 | 模拟方式 | 防御机制 | 预期结果 | 验证状态 |
|---------|---------|---------|---------|---------|
| **SYN Flood** | hping3 发送大量 SYN 包 | 滑动窗口频率检测 | 拦截 > 99% 攻击流量 | ✅ 已验证 |
| **HTTP Flood** | ab 模拟高频 HTTP 请求 | 滑动窗口 + 布隆过滤器 | 超阈值 IP 被拦截并加入黑名单 | ✅ 已验证 |
| **暴力破解** | 脚本模拟连续登录尝试 | 攻击监控（5 次失败→锁） | 第 6 次请求被拦截 | ✅ 已验证 |
| **重放攻击** | 捕获合法请求后重复发送 | HMAC 一次性令牌 | 第二次请求因令牌已用被拒绝 | ✅ 已验证 |
| **Prompt Injection** | 构造注入指令测试 | InputGuardrail | 40+ 规则命中，统一返回 403 | ✅ 已验证 |
| **SQL 注入** | 输入 `' OR 1=1--` 等载荷 | 参数化查询 + 注入检测 | 双重防护，攻击无效 | ✅ 已验证 |
| **XSS 攻击** | 输入 `<script>alert(1)</script>` | XSS 检测规则 + CSP 头 | 拦截 + 浏览器拒绝执行 | ✅ 已验证 |
| **越权访问** | 无 Token 调用管理端 API | JWT 认证中间件 | 返回 401 Unauthorized | ✅ 已验证 |
| **CSRF** | 伪造跨域请求 | CSRF Token 机制 | 无有效 Token 的请求被拒绝 | ✅ 已验证 |
| **零宽字符注入** | 嵌入 U+200B 等零宽字符 | Guardrail 零宽字符清洗 | 字符被清除，后续规则正常检测 | ✅ 已验证 |

#### 5.4.2 攻防演练结果总结

- **10 类攻击场景全部被成功防御**
- 无任何一条攻击载荷穿透到业务逻辑层
- 防护机制之间无冲突（白名单 IP 不受限流影响，关键服务始终可用）
- 误拦率 < 0.1%（仅布隆过滤器固有 0.001% 误判）

### 5.5 知识产权

| 类型 | 状态 |
|------|------|
| 发明专利 | 1 项（已受理） |
| 软件著作权 | 2 项（申请中） |
| 学术论文 | 2 篇（初稿） |

---

## 六、国产化适配方案

### 6.1 操作系统适配

- 系统可运行于**银河麒麟操作系统（Kylin OS）**
- Node.js 运行时无国产依赖
- 数据库适配 MySQL / SQLite，均可在麒麟系统运行

### 6.2 大模型适配

- 可插拔 LLM 架构支持接入国产大模型：
  - **通义千问（Qwen）：** 通过 vLLM 本地部署或 DashScope API
  - **文心一言：** 通过 custom 适配器接入
  - **星火认知：** 通过 custom 适配器接入
- 默认使用 `rule_engine`（零依赖规则引擎），无需联网即可运行

### 6.3 数据安全

- 所有数据处理在本地完成，不依赖外部 API
- 知识库数据存储在本地 MySQL/SQLite
- 交互日志本地持久化，支持审计导出
- 支持内网隔离部署

### 6.4 答辩核心话术

> "我们的智能体框架完全自主构建，不依赖任何第三方平台或云端 API，实现了从模型推理到工具调用的全链路国产化部署。系统可运行于麒麟操作系统，所有数据在本地处理，满足机场对数据安全和自主可控的合规要求。未来无论机场自研模型还是更换国产算力平台，我们的智能体框架只需修改配置文件中的模型地址即可无缝适配。"

---

## 七、安全性分析

### 7.1 纵深防御体系

| 层级 | 防护措施 |
|------|---------|
| 网络层 | 滑动窗口频率检测 + 布隆过滤器黑名单 |
| 认证层 | HMAC 一次性令牌 + JWT 管理员认证 + CSRF 保护 |
| 输入层 | Prompt Injection / SQL 注入 / XSS / 命令注入检测 |
| 执行层 | 动作白名单 + 最小权限 + 用户上下文隔离 |
| 输出层 | 内部信息泄露过滤 |
| 审计层 | 全链路审计日志 + 安全分析 Agent |

### 7.2 认证安全

- JWT 纯 Node.js 实现（HMAC-SHA256 签名 + 恒定时间比较）
- 管理员密码 SHA-256 哈希存储
- 支持动态增删管理员、修改密码
- 所有管理端 API 强制认证

### 7.3 数据保护

- 数据库密码仅从环境变量读取（生产环境）
- 值机操作 HMAC 令牌保护，防重放
- 参数化 SQL 查询，防注入
- PII 数据自动脱敏（身份证号、手机号）

---

## 八、应用场景与推广价值

### 8.1 目标用户

| 用户 | 痛点 | 本方案价值 |
|------|------|-----------|
| 中小机场 | 无力承担重型安全设备 | 纯软件方案，低成本部署 |
| 老年旅客 | 不会操作自助终端 | 老年模式 + 语音交互 |
| 安全运维人员 | 手动分析日志，效率低 | Agent 自动分析 + 智能阈值建议 |
| 民航监管部门 | 国产化替代压力 | 自主可控，满足合规 |

### 8.2 竞赛演示方案

#### 8.2.1 现场 Demo 流程（5 分钟）

| 步骤 | 内容 | 预期展示效果 | 时长 |
|------|------|------------|------|
| 1 | 启动系统，展示健康检查 | `/api/health/db` 返回连接成功 | 10s |
| 2 | 旅客端语音交互 | 说"我的航班在哪登机"→ 系统返回登机口信息 | 30s |
| 3 | 老年模式切换 | 一键切换，展示大字体、大按钮界面 | 15s |
| 4 | 模拟 DDoS 攻击 | 后台 ab 发送高频请求 → 安全面板实时显示攻击拦截 | 40s |
| 5 | Prompt Injection 攻击演示 | 输入注入指令 → 系统拦截并记录日志 → 展示 Guardrail 生效 | 30s |
| 6 | 智能阈值调整演示 | 攻击加剧 → 安全 Agent 自动建议降低阈值 → 管理员一键应用 | 40s |
| 7 | 情感交互演示 | 模拟焦急旅客："我赶不上飞机了怎么办" → 系统先安抚情绪再查询航班 | 30s |
| 8 | 安全审计日志展示 | 展示完整审计链路：攻击→拦截→分析→处置 | 20s |
| 9 | 国产化声明 | 展示无第三方依赖、可在麒麟系统运行 | 15s |

#### 8.2.2 演示环境准备

- **在线 Demo：** 腾讯云公网部署（http://81.70.228.232:3000）
- **离线备用：** 本地 Node.js 环境 + SQLite 数据库，无需外网
- **攻击演示工具：** ab（Apache Bench）、curl
- **录屏备份：** 所有 Demo 步骤录制视频，防止现场环境异常

### 8.3 可扩展方向

1. **硬件适配：** 通过本地桥接服务调用身份证读卡器、登机牌打印机（CUSS 2.0 兼容）
2. **多机场部署：** 支持多节点集群 + Nginx 负载均衡
3. **更多安全能力：** WAF、API 网关、异常行为检测
4. **更多智能体能力：** 多轮对话、跨语言服务（外语旅客）

---

## 九、项目优势总结

| 维度 | 优势 | 竞品对比 |
|------|------|---------|
| **安全性** | 6 项 DDoS 防护 + 3 道 Agent 防线 + 10 类攻防演练通过 | 多数方案仅关注单一层面 |
| **智能化** | 自研智能体框架，意图理解 + 情感分析 + 知识检索 | 竞品依赖第三方 AI 平台 |
| **国产化** | 全链路自主可控，运行于麒麟系统 | LangChain/CrewAI 为海外产品 |
| **轻量化** | 纯软件方案，性能损耗仅 7.1% | 硬件方案成本高、部署重 |
| **适老化** | WCAG AA 级标准，一键老年模式 | 多数终端无适老化设计 |
| **实用性** | 已在腾讯云公网部署，163 项测试通过 | 很多竞赛作品缺乏实际验证 |
| **合规性** | 7 项国家政策依据，覆盖安全/数据/适老化/国产化 | 多数方案无明确政策对应 |

---

## 十、团队与分工

| 成员 | 分工 |
|------|------|
| 杨梓瑜 | 智能体架构设计、安全模块开发、Docker 部署 |
| 雷丰毅 | 专利撰写、技术方案细化 |
| 张一 | 论文撰写、学术调研 |
| 段琼馨 | 前后端开发、安全管理面板 |
| 王晓恩 | 国产大模型调研、管理端页面 |
| 苏晨曦 | 国产操作系统适配调研 |

---

## 十一、项目进度与计划

| 阶段 | 时间 | 里程碑 |
|------|------|--------|
| 需求分析与技术调研 | 5 月 | 确定"安全+智能体"方向，完成技术选型 |
| 核心功能开发 | 5 月–6 月 | 6 项 DDoS 防护、JWT 认证、智能体框架 |
| Agent 模块开发 | 6 月 1–15 日 | 状态机调度器、输入过滤、意图分类、知识库检索 |
| 安全加固与修复 | 6 月 16–27 日 | 认证中间件、数据库适配、代码审核、漏洞修复 |
| 申报书撰写 | 6 月 28–29 日 | 完成申报书并迭代优化 |
| **提交截止** | **7 月 1 日** | **完成最终版提交** |
| 演示 Demo 准备 | 7 月上旬 | 录屏、现场环境测试 |
| 竞赛答辩 | 待定 | 现场演示 + 答辩 |

---

## 参考文献

1. VeriGuard: Enhancing LLM Agent Safety via Verified Code Generation (2025)
2. A Multi-Agent LLM Defense Pipeline Against Prompt Injection Attacks (IEEE WIECON-ECE 2025)
3. Prompt Infection: LLM-to-LLM Prompt Injection within Multi-Agent Systems (ICLR 2025)
4. LangGraph: State Machine-based Agent Framework
5. CUSS 2.0 机场自助服务标准
6. 《中华人民共和国网络安全法》
7. 《中华人民共和国数据安全法》
8. 《关键信息基础设施安全保护条例》
9. 《"十四五"民航高质量发展专项规划》
10. 《互联网网站适老化通用设计规范》（工信部，2021）

---

*本申报书最终版本（v1.1），2026-06-29 完成。*
